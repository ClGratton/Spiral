# Core Ownership

Core owns application lifecycle, logging, assertions, layers, windows, command-line args, and basic utility types.

Core must stay lightweight and must not depend on renderer, scene, assets, scripting, or editor code.
